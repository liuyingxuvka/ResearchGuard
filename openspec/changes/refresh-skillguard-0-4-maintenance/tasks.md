## 1. Current Planning and Model Boundary

- [x] 1.1 Upgrade the repository's FlowGuard project record to the installed current engine and confirm project audit passes.
- [x] 1.2 Record an existing-model preflight that reuses the four current skill-contract models and identifies old standalone repositories as protected external authority risks.
- [x] 1.3 Add a model-derived StructureMesh and TestMesh for the single suite, four consumer entrypoints, and eight target-declared checks.
- [x] 1.4 Record the DevelopmentProcessFlow order, freshness domains, hard stops, and no-install/no-publish claim boundary.

## 2. SkillGuard Current Author Authority

- [x] 2.1 Add the current SkillGuard 0.4 author-project manifest and managed author block for exactly four members in `unit:researchguard-suite`.
- [x] 2.2 Regenerate each member's compiled contract and check manifest directly from its unchanged target-owned source contract.
- [x] 2.3 Confirm current maintainer audit and compiler parity for all four members with no compatibility or inferred domain route.

## 3. Validation and Consumer Projection

- [x] 3.1 Freeze the same-unit eight-check validation inventory with one declared owner per check and explicit private run/evidence roots.
- [x] 3.2 Run affected FlowGuard model checks and the four consumer-contract checks.
- [x] 3.3 Run the four member-owned native regression suites and aggregate current SkillGuard evidence without cross-unit receipt reuse.
- [x] 3.4 Compare each clean source consumer projection with the installed consumer tree, reporting content parity and release-manifest gaps separately without activating installation.

## 4. Closure

- [x] 4.1 Validate the OpenSpec change and record task status without archiving it.
- [x] 4.2 Recheck peer/Git state, current evidence, skipped cleanup/installation/publication work, and the bounded final claim.
