## 1. Admission v2 and minimum-sufficient routing

- [x] 1.1 Add the current source-bound task-facts schema, structural validation, condition evaluation, and deterministic admission-index projection.
- [x] 1.2 Replace all four member admission contracts with positive, required, forbidden, first-action, and first-reference declarations that derive their own admission evidence.
- [x] 1.3 Replace umbrella admission-v1 input with task-facts input while preserving direct-member bypass, recursion blocking, typed handoff, and no fallback; derive the unique minimum sufficient member set.
- [x] 1.4 Add and validate the current composition schema: exact derived member set, order/dependencies, per-member responsibility, input/output handoffs, single field ownership, and overall claim boundary; emit planning evidence without claiming native execution.
- [x] 1.5 Add routing and root-CLI known-bad tests for single-member, necessary pair/multi-member, over-selection, responsibility/field conflict, missing order/dependency, placeholder spans, stale facts, zero match, and direct bypass.

## 2. Narrow consumer entry surfaces

- [x] 2.1 Generate and verify `skills/researchguard/references/member-admission-index.md`, then reduce the umbrella skill to suite routing, index loading, minimum-sufficient gates, declarative composition, handoff, and claim boundary.
- [x] 2.2 Reduce LogicGuard's entry shell and move existing general argument, task maturation, broad closure, structural contribution, citation, commands, safety, and template-pack material into trigger-owned references without losing its five current internal routes.
- [x] 2.3 Reduce SourceGuard's entry shell and move model protocol, retrieval workflow, task iteration, broad closure, commands, safe output, and template-pack material into trigger-owned references while retaining one native route.
- [x] 2.4 Reduce TraceGuard's entry shell and move general trace, task iteration, depth/closure, commands/handoffs, safety, and template-pack material into trigger-owned references while retaining the current case-library route and inference protocol.
- [x] 2.5 Keep ExperimentGuard independent and compact while adding admission-v2 wording and explicit conditional deepening triggers.
- [x] 2.6 Update all five `agents/openai.yaml` prompts to route and load first, and to trigger—rather than unconditionally start—the existing strict task-local loop.

## 3. Prompt and model governance

- [x] 3.1 Add a target-owned prompt-bundle/load-graph manifest, checker, and known-bad fixtures covering budgets, generated-index freshness, trigger edges, ownership, missing references, and prohibited sibling loads.
- [x] 3.2 Update the ResearchGuard FlowGuard suite model, JSON projection, runner, and common member skill-contract model for admission-v2 and conditional-load obligations.
- [x] 3.3 Update `scripts/check_researchguard_suite.py` and focused suite tests to consume the new admission and prompt evidence.

## 4. SkillGuard author contracts and patch identity

- [x] 4.1 Update `scripts/build_skillguard_contracts.py` so prompt/load checks and all changed components have exact same-unit owner and selector coverage.
- [x] 4.2 Regenerate the five contract-source files, compile their current contracts/check manifests with the installed SkillGuard author toolchain, and verify no author material enters consumer projections.
- [x] 4.3 Advance package, module, executable/JSON suite-model, check-script, contract claim, and changelog identity to patch version `0.4.1`.

## 5. Affected validation and handoff

- [x] 5.1 Validate the OpenSpec change strictly and run focused routing, root-CLI, prompt/load-graph, native suite, and member skill-contract tests.
- [x] 5.2 Run the affected FlowGuard suite/model checks and SkillGuard affected contract compilation/audit without starting the final full maintenance-unit gate.
- [x] 5.3 Re-read repository status, preserve parallel-agent writes and `.flowguard/evidence/`, record KB postflight if warranted, and report exact evidence, skipped final gates, residual risk, and claim boundary.
