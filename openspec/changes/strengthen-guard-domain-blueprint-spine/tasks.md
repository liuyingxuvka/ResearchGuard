## 1. Authority, Coordination, and Frozen Boundaries

- [x] 1.1 Re-read `AGENTS.md`, repository coordination state, `git status`, active peer claims, and the current untracked `.flowguard/evidence/` ownership before the first write; record that peer evidence and unrelated changes are excluded from this change.
- [x] 1.2 Freeze one implementation baseline containing the ResearchGuard source commit, Python/package identity, the five maintained skill ids, the four native member ids, current native schema ids, prompt-manifest fingerprint, and current observed FlowGuard snapshot identity.
- [x] 1.3 Confirm `openspec/specs/researchguard-member-model-loops/spec.md` and `openspec/specs/researchguard-narrow-entry-loading/spec.md` are the current modified-capability bases and prevent historical exact-one or v0.1.x change artifacts from becoming implementation authority.
- [x] 1.4 Assign disjoint native ownership for `src/researchguard/{experiment,logic,source,trace}` and one integration owner for shared `routing.py`, root CLI/suite files, prompt manifest, `.flowguard`, `.skillguard`, package, install, and release surfaces.
- [x] 1.5 Freeze current-format migration decisions for every added or changed field, including exact retired-field dispositions and the rule that normal runtime gains no compatibility reader, converter, alias, dual output, retry, or fallback.

## 2. ExperimentGuard Native Domain Blueprint

- [x] 2.1 Extend `src/researchguard/experiment/schema.py` with current-schema experiment design blocks, procedure steps, constraints, external execution ownership, and typed input, manipulation, observation, and outcome ports; update `__init__.py` exports only for the exact public types.
- [x] 2.2 Add `src/researchguard/experiment/blueprint.py` to validate one rooted acyclic design hierarchy, parent consumption of child outputs, independent target-universe coverage, deterministic model identity, deepest proven layer, and first unresolved gap.
- [x] 2.3 Update `src/researchguard/experiment/engine.py` so the existing finite recommendation matrix consumes the same hypothesis, candidate, observation-port, and outcome identities as the design blueprint without adding another recommendation or scoring path.
- [x] 2.4 Extend `src/researchguard/experiment/cli.py` with native blueprint check, impact, reverse-trace, and deterministic export operations that never execute an external experiment.
- [x] 2.5 Add current known-good, hierarchy/interface known-bad, omitted-universe, stale-fingerprint, and external-owner fixtures for ExperimentGuard and directly replace maintained retired shapes.
- [x] 2.6 Add `tests/experiment/test_blueprint_design.py` covering one-root hierarchy, dangling/cyclic relations, missing manipulation or observation ports, unconsumed outputs, independent-universe omission, deterministic round trip, affected hypothesis pairs, reverse trace, and external non-execution.
- [x] 2.7 Extend `tests/experiment/test_recommendation.py` to prove the same design identities drive minimum-set recommendation, observations, model-miss revision, and holdout closure, while preserving all existing recommendation boundaries.
- [x] 2.8 Bind every consumed child observation/outcome to one parent input with exact schema/refinement, payload/model/result/task identities, and a native receipt resolved from immutable producer bytes; reject caller-invented receipt ids and self-hashes.
- [x] 2.9 Require a provider-neutral externally owned expected-target anchor before ExperimentGuard modeling; replay only its exact locator/bytes in fresh and serialized processes, keep direct use without an anchor unverified, and require a separate anchor for an alternate target.
- [x] 2.10 Remove public/generic target-authority issuance and production inline/candidate authoring; bind private authority to the expected anchor, reject coherent raw-bytes/model/universe/native-evidence co-shrink under the frozen anchor, and gate impact/reverse trace on exactly one current ExperimentGuard qualification.

## 3. LogicGuard Native Domain Blueprint

- [x] 3.1 Extend `src/researchguard/logic/model.py` with a current block-interface binding that names the child output claim, parent input node, classifications, scope, consumer status, and consumed fingerprint without creating a second hierarchy authority.
- [x] 3.2 Strengthen `src/researchguard/logic/validator.py` and `hierarchy.py` so every required parent input has one compatible producer, every material child output is consumed or explicitly dispositioned, and conflicting durable parent projections fail instead of remaining warnings.
- [x] 3.3 Add `src/researchguard/logic/artifact_inventory.py` with an independently fingerprinted adapter-neutral inventory of artifact units, containment, locators, content identities, assets, and parse dispositions; keep model contents from defining the denominator.
- [x] 3.4 Add `src/researchguard/logic/blueprint.py` to bind artifact units to argument blocks, nodes, sources, citations, receipts, and interface relations and to report structural versus content/resource reconstruction layers separately.
- [x] 3.5 Update `src/researchguard/logic/structured_artifact.py` and `execution_depth.py` to consume the independent inventory and immutable resolved leaf-interface receipts without allowing an aggregate, rich sibling branch, or caller-invented receipt id to hide an unresolved required unit.
- [x] 3.6 Extend `src/researchguard/logic/mesh_invalidation.py` and the existing ModelMesh path so changed units, claims, sources, or interface fingerprints invalidate only declared blocks, ancestors, citations, overlays, synthesis projections, and receipts.
- [x] 3.7 Extend `src/researchguard/logic/cli.py` with member-native blueprint check, impact, reverse-trace, and deterministic export operations over the same LogicGuard model and receipts.
- [x] 3.8 Add `tests/logic/test_blueprint_interfaces.py` for producer/consumer closure, unconsumed outputs, incompatible ports, conflicting hierarchy ownership, deepest layer, and first-gap behavior.
- [x] 3.9 Add `tests/logic/test_artifact_inventory.py` for independent denominator omissions, parse gaps, structural/content claim separation, content fingerprint changes, and deterministic export/reload.
- [x] 3.10 Extend `tests/logic/test_schema.py`, `test_hierarchy.py`, `test_execution_depth.py`, `test_mesh_invalidation.py`, and `test_structured_artifact.py` to prove native semantic, impact, and existing ModelMesh behavior remain current.
- [x] 3.11 Replace self-fingerprinted artifact authority with an externally admitted expected-target anchor and immutable model/interface/leaf/depth/owner receipt resolution; reject coherent inventory/realization/raw-bytes/native-evidence co-shrink and never-published receipts.
- [x] 3.12 Replay LogicGuard artifact targets and every qualifying receipt from their exact immutable locators in fresh and serialized processes; keep absent/unavailable external admission unverified and require a new anchor for an alternate artifact target.
- [x] 3.13 Remove public/generic target-authority issuance and production inline/inventory authoring; bind private authority to the expected anchor, gate impact/reverse trace on exactly one current LogicGuard qualification, and de-duplicate blocked reverse-trace gaps.

## 4. SourceGuard Native Domain Blueprint

- [x] 4.1 Promote the typed relationship object into the current `src/researchguard/source/schema.py` authority and replace raw `graph_edges: list[dict]` loading with immediate type, endpoint, and relation validation.
- [x] 4.2 Update `src/researchguard/source/graph.py` to consume only current typed edges, reject raw or dangling bypass shapes, and preserve deterministic adjacency and graph identity.
- [x] 4.3 Extend SourceRecord, EvidenceAnchor, and observation identity with source-content, retrieval-request, provider/revision, anchor-content, source-content, extractor/revision, locator, and observation fingerprints required by `sourceguard-information-blueprint`.
- [x] 4.4 Add `src/researchguard/source/blueprint.py` to validate the objective-to-target-unit-to-gap-to-source-role-to-search-action-to-source-to-anchor-to-claim-use/handoff chain and exact parent consumption of child outputs.
- [x] 4.5 Extend `src/researchguard/source/depth.py` and `guard_contract.py` so the independent universe covers target units, gaps, source roles, lineage slots, anchor/bridge requirements, handoffs, native good/bad cases, and the first unresolved layer.
- [x] 4.6 Extend `src/researchguard/source/task_iteration.py` and `handoff.py` so source or anchor changes reopen only exact consuming gaps, stop decisions, claim uses, and member handoffs and preserve unrelated branches.
- [x] 4.7 Extend `src/researchguard/source/cli.py` with member-native blueprint check, impact, reverse-trace, and deterministic export operations without adding final argument, storyline, causal, or truth judgment.
- [x] 4.8 Add `tests/source/test_blueprint_graph.py` covering load-time typed-edge validation, invalid endpoints, complete interfaces, independent-universe omissions, content/extractor drift, affected-only invalidation, reverse trace, and deterministic round trip.
- [x] 4.9 Extend `tests/source/test_schema.py`, `test_semantic_gap_depth.py`, `test_task_iteration.py`, `test_model_guard_binding.py`, and `test_handoff.py` to retain current source/anchor qualification, lineage, stop, purpose, and closure behavior.
- [x] 4.10 Bind the information target to an externally admitted expected-target anchor and immutable model/interface/purpose/contract/owner receipts; reject coherent target-tree/universe/raw-bytes/native-evidence co-shrink and never-published receipts.
- [x] 4.11 Replay SourceGuard target trees, obligations, and qualifying receipts from exact immutable locators in serialized/fresh processes; keep direct use without external admission unverified and require a new anchor for an alternate target.
- [x] 4.12 Remove public/generic target-authority issuance and production inline/graph authoring; bind private authority to the expected anchor and gate impact/reverse trace on exactly one current SourceGuard qualification.

## 5. TraceGuard Native Domain Blueprint

- [x] 5.1 Add current object, source-revision, locator, and normalizer/extractor fingerprint bindings required for blueprint freshness to `src/researchguard/trace/schema.py` without accepting caller-authored inference outputs.
- [x] 5.2 Add `src/researchguard/trace/blueprint.py` to deterministically project one investigation-to-storyline-hypothesis-to-trace-to-sequence/stage-to-event-to-evidence-to-source hierarchy from current native identities.
- [x] 5.3 Implement typed EvidenceFact-to-Event, Event-to-Trace, Trace-to-Hypothesis, and Hypothesis-to-BoundedClaim/Handoff interfaces with required parent inputs and explicit unconsumed-output dispositions.
- [x] 5.4 Update `src/researchguard/trace/inference/compiler.py` and `inference/projection.py` so blueprint projection consumes the existing canonical inference receipt and cannot introduce a second score, solver, threshold, compatibility path, or fallback.
- [x] 5.5 Extend `src/researchguard/trace/storyline_depth.py` and `task_iteration.py` to include hierarchy/interface fingerprints, exact affected objects, live alternatives, causal boundaries, deepest proven layer, and first unresolved gap.
- [x] 5.6 Extend `src/researchguard/trace/cli.py` with member-native blueprint check, impact, reverse-trace, and deterministic export operations over the canonical model and receipt.
- [x] 5.7 Add `tests/trace/test_blueprint_hierarchy.py` covering deterministic one-root projection, invalid references, missing evidence inputs, unconsumed events, receipt freshness, affected-only propagation, alternative preservation, reverse trace, and round trip.
- [x] 5.8 Extend `tests/trace/test_traceguard.py`, `test_inference_kernel.py`, `test_storyline_depth.py`, and `test_task_iteration.py` to prove no-fallback inference, deterministic replay, lineage independence, causal boundaries, perturbation/depth, and holdout behavior remain native and current.
- [x] 5.9 Require model/interface/canonical/purpose/depth/owner receipts to resolve immutable producer bytes and bind exact producer/result/model/checker/request/input/task/status identities; reject stale, foreign, self-hashed, and never-published receipts.
- [x] 5.10 Require an externally admitted expected-target anchor and replay TraceGuard stable-object targets and qualifying receipts from exact immutable locators in fresh and serialized processes; reject coherent model/universe/raw-bytes/native-evidence co-shrink and require a new anchor for an alternate target.
- [x] 5.11 Remove public/generic target-authority issuance and production inline/model authoring; bind private authority to the expected anchor, keep direct use without external admission unverified, and gate impact/reverse trace on exactly one current TraceGuard qualification.

## 6. ResearchGuard Envelope, Handoffs, and Integration

- [x] 6.1 Add `src/researchguard/model_envelope.py` with strict current `MemberModelEnvelope`, `HandoffFieldContract`, consumer acknowledgement, fingerprint, terminal, gap, and claim-boundary validation and no member-payload parser.
- [x] 6.2 Extend `src/researchguard/routing.py` composition validation to require current envelopes for selected steps, exact field schemas and fingerprints, one producer, declared consumers, consumer status, and same-task receipt lineage.
- [x] 6.3 Implement composition-level affected-only invalidation from changed envelope/handoff fingerprints, marking only direct and transitive declared consumers stale and blocking unknown ownership without `run all`.
- [x] 6.4 Implement composition-level reverse trace from overall claim boundary through step, handoff, producer/consumer, envelope, native receipt, and source-bound responsibility span, stopping at the member-native receipt boundary.
- [x] 6.5 Extend `src/researchguard/cli.py` and `suite.py` to project envelope, handoff, stale, blocker, and reverse-trace results while retaining the existing minimum-sufficient route and same native direct owner.
- [x] 6.6 Add `tests/test_member_model_envelope.py` covering missing/stale/foreign envelopes, payload opacity, producer uniqueness, consumer acknowledgement, terminal preservation, exact invalidation, and reverse trace.
- [x] 6.7 Extend `tests/test_suite_routing.py` for envelope-aware necessary pair and three-member compositions, over-selection, exact native owners, unknown-impact blocking, and no retry/fallback behavior.
- [x] 6.8 Add `tests/test_guard_blueprint_integration.py` with one current blueprint fixture per member, a typed cross-member handoff, upstream fingerprint change, exact downstream stale set, reverse trace, visible member failure, and proof that no shared domain solver is imported.
- [x] 6.9 Extend `tests/test_root_cli.py` and public API tests for direct and umbrella-referenced blueprint operations and exact native owner parity.
- [x] 6.10 Propagate each externally admitted expected-target anchor and complete immutable native-receipt locator set through the member qualification, owner attestation, envelope, handoff, composition, and reverse trace; preserve parent gaps and block readiness when any anchor or receipt is unresolved.
- [x] 6.11 Remove every public generic target/attestation/receipt issuer; bind the closed registries to exact external admission owner and member checker capability/version/entrypoint, and require each member to replay its anchor plus opaque request/input/result/resolved receipts/terminal before `composition_ready`.
- [x] 6.12 Make composition impact and reverse trace qualification-first over the complete selected set, including all four members and recursive handoffs; atomically suppress ordinary projections and return non-success CLI status whenever composition is not ready.
- [x] 6.13 Extend `NativeReceiptExpectation` and resolution with exact producer id/version, signature algorithm, signing-key id, public-key fingerprint, and producer-descriptor fingerprint; reject alternate registered producers and keys.
- [x] 6.14 Add one deterministic content-addressed portable composition bundle plus a fresh-process bundle-only handoff qualifier that consumes no repository checkout or process-global authority residue.
- [x] 6.15 Bind every newly claimed composition and handoff behavior case to `Input`, `PreState`, permitted `Output`, `PostState`, `Effect`, protected `Failure`, and native `Oracle`.
- [x] 6.16 Replace import-time test producer registration and real-home authority writes with explicit temporary authority roots and scoped registries whose teardown removes only their own exact files.
- [x] 6.17 Add the generic production external-domain-DNA API and CLI with version/hash/locator binding, parent/child hierarchy, four member-owned transition models, object bindings, acyclic dependency graph, behavior-ID query, affected-only impact, and reverse trace.
- [x] 6.18 Replay every external-object projection and its pre-state, post-state, effect, protected failure, and claim boundary through its fixed SourceGuard, TraceGuard, LogicGuard, or ExperimentGuard evaluator; separately distinguish reusable capability-fixture replay from target-native replay bound to the same external target/scope/material anchor, and reject caller-rebound output or state even when all member evidence and the outer artifact identity are recomputed. Reopened after independent audit found only generic capability fixtures rather than target-native replay.
- [x] 6.19 Add a canonical reusable `Attention Is All You Need` arXiv v7 target model and regression that bind the frozen PDF/source hashes and preserve the 41.8-versus-41.0 conflict at exact TeX locators without absolute paths, embedded source bytes, or silently choosing a value.
- [x] 6.20 Direct-replace the partial external-domain-DNA path so `researchguard domain-dna` is only a facade over current SourceGuard, TraceGuard, LogicGuard, and ExperimentGuard native blueprint, target-authority, owner-attestation, behavior-manifest, envelope, and portable-composition owners; delete the four conflict-specific `domain_dna.py` semantic evaluators and retain no alias, compatibility reader, fallback, dual schema, or fifth success owner.
- [x] 6.21 Add one independently replayed closed target-denominator owner that inventories the complete declared material boundary before reading the candidate DNA. Require exact source-authority closure for every extraction `source_id`, one typed disposition for every independently inventoried file, object, structure node, anchor, and target item, and reject caller-shrunk or foreign artifacts, structures, roles, claims, events, experiments, or bindings even after every candidate/member/outer fingerprint is coherently recomputed. Reopened after a foreign `source_id` could be re-signed and qualify.
- [x] 6.22 Replace the label-only parent with one machine-checkable `Input x State -> Set(Output x State)` composition FunctionBlock. Freeze exact child order, aggregate inputs/state/outputs/effects, exhaustive child-input mappings, exhaustive child-output terminal dispositions, handoffs, and exactly one owner per field; reject every orphan owned id, input, output, binding, object, structure node, claim, event, experiment, or gap.
- [x] 6.23 Bind the complete structure/object hierarchy to exact member inputs, outputs, behaviors, evidence, tests, native receipts, and semantic handoffs. Require Trace to consume Source roles/anchors/gaps, Logic to consume Trace events/storylines/gaps, and Experiment to consume Logic claims/edges/gaps so a semantic mutation changes or blocks every declared downstream consumer. Make root, section, object, and member impact/reverse trace follow only declared edges, make a root change reach all declared descendants/member models, preserve local affected-only closure, and atomically block unknown ids. Reopened after downstream bytes remained unchanged under upstream semantic mutations.
- [x] 6.24 Prove one generic production topology for conflicting and non-conflicting paper-like targets and a non-paper test/workflow target. Remove Attention-specific, 41.x-specific, forced-conflict, paper-title, arXiv, TeX, or numeric-value decisions from production semantics; member-native owners alone derive their domain conclusions.
- [x] 6.25 Separate candidate/bundle self-consistency from material qualification and evidence authority. Bind each code/material/test/oracle/evidence relation to one exact target-native owner, exact external target/scope/material anchor, and independently replayed terminal receipt; reject invented tests, oracles, evidence, native receipts, candidate specs, bundle hashes, self-hashes, caller-invented roles/coverage, missing LogicGuard scope limits, orphan Trace events/Logic claims, locator-role rebinding, and top-level boundary overclaim. Reopened after entirely invented bindings could be made current and qualify.
- [x] 6.26 Package the canonical external-domain-DNA examples and their README on a clean installed distribution surface, expose compact default plus exact one-id member/behavior/object/impact/reverse queries, and keep default output below 16 KiB and at least ten times smaller than the full bundle when the full bundle is at least 160 KiB.
- [x] 6.27 Add exact attack regressions for: all bindings removed; independent artifact denominator shrunk 4-to-2 or one file omitted; structure shrunk 4-to-1 or one node omitted; foreign SourceGuard `source_id`; SourceGuard role/coverage invented; upstream Source/Trace/Logic semantic mutation with unchanged downstream bytes; invented test/oracle/evidence/native receipts; generic capability fixture substituted for target-native replay; LogicGuard scope limits removed; Trace event or Logic claim omitted while owned ids remain; table anchor rebound to a prose locator; top-level boundary overclaimed; consistent non-conflict target; and all-candidate/member/outer fingerprints coherently recomputed.
  - Evidence: the rebuilt 25-file external material root closed all 19 external-DNA scenarios plus 2 compiler-boundary tests; two consecutive canonical compilations produced byte-identical source and packaged resources, and an independent read-only recheck expanded five attack groups to 8 passing cases without reproducing the four prior P0 classes.
- [x] 6.28 Separate material-denominator completeness from semantic-scope completeness. Require one explicit target purpose/scope, exact included question/structure ids, exclusions, recursive expansion frontier, and four member scope bindings with exact input/output denominators; keep `dna_qualified` bounded to that declared scope and reject a 25-of-25 file inventory as proof of whole-paper understanding.
- [x] 6.29 Add a bounded process-local read-only native-replay cache keyed by the complete portable bundle bytes plus normalized exact trust set. Return defensive copies, cap capacity, miss on altered bundle/trust, cache no material result, and prove every material-qualified call independently re-enumerates and hashes current target bytes.
- [x] 6.30 Carry the closed LogicGuard, SourceGuard, TraceGuard, and ExperimentGuard native proof inputs inside the portable bundle, replay them without repository, compiler-temporary, target-tree, or user-profile reads, bind one immutable `(member_id, receipt_id)` identity to one signed receipt inside local authority, and state explicitly that a single isolated bundle does not prove cross-bundle producer non-equivocation.
- [x] 6.31 Replace candidate-derived external-object completeness with one target-neutral signed scope-authority schema whose independently produced provenance, target/version/material inventory, scope/purpose/completion, structure semantics, anchor roles/selectors, four member input/output kinds, handoffs, exclusions/frontier, and scope limits define the qualification denominator.
- [x] 6.32 Keep every scope-authority private key outside production and packaged resources. Runtime SHALL only validate the carried public descriptor/signature, and SHALL require the caller to trust the exact authority fingerprint or producer-descriptor fingerprint out of band; carrying a signed record inside the bundle MUST NOT grant trust.
- [x] 6.33 Compare every authority requirement with candidate produced, consumed, disposed, missing, unexpected, and mismatched objects; compare the parent FunctionBlock with the authority-derived external input/output/disposition interface as well as candidate-internal exhaustiveness.
- [x] 6.34 Split qualification into `material_inventory_verified`, `member_native_replay_verified`, `candidate_internal_consistency`, `scope_authority_licensed`, `scope_obligation_coverage`, and `dna_qualified`; keep missing/untrusted authority or incomplete coverage at `candidate_model`/`dna_self_consistent`, never `dna_qualified`.
- [x] 6.35 Replace paper-line-only source occurrences with one typed selector contract supporting current `line_pattern` and `json_pointer` bindings, exact semantic roles, distinct occurrence identities, and material replay without target-kind hardcoding.
- [x] 6.36 Add attack regressions for four-member synchronized shrink, zero included structures, coherent table/prose structure deletion, real-line semantic rebinding, duplicate occurrence binding, unconstrained LogicGuard scope, and synchronized target-kind/Source-role rewrite; require authority-required gaps in qualification, impact, and reverse.
- [x] 6.37 Add one legitimately smaller independently authorized scope and one non-paper JSON-pointer workflow target that qualify only with explicit external authority trust, plus clean-wheel/package/private-key-absence checks.

## 7. Skill Prompts and Conditional Loading

- [x] 7.1 Add a concise envelope/ownership trigger to `skills/researchguard/SKILL.md` and `agents/openai.yaml`, and add `skills/researchguard/references/member-model-envelope.md` with full opaque-envelope, handoff, impact, trace, and claim-boundary guidance.
- [x] 7.2 Add a concise blueprint trigger to `skills/experimentguard/SKILL.md` and `agents/openai.yaml`, and add `skills/experimentguard/references/experiment-model-protocol.md` with native design hierarchy, port, recommendation, impact, trace, and external-owner guidance.
- [x] 7.3 Add a concise blueprint trigger to `skills/logicguard/SKILL.md` and `agents/openai.yaml`, add `skills/logicguard/references/domain-blueprint-contract.md`, and align model-deepening and structured-artifact references with interface and independent-inventory requirements.
- [x] 7.4 Add a concise blueprint trigger to `skills/sourceguard/SKILL.md` and `agents/openai.yaml`, add `skills/sourceguard/references/information-blueprint.md`, and align source-model and task-iteration references with the sole typed graph and content identity.
- [x] 7.5 Add a concise blueprint trigger to `skills/traceguard/SKILL.md` and `agents/openai.yaml`, add `skills/traceguard/references/trace-blueprint-contract.md`, and align general-trace, inference, and depth references with deterministic hierarchy and canonical receipt consumption.
- [x] 7.6 Update `researchguard/prompt_bundle_manifest.json` with exact selected-member trigger/reference edges and no eager sibling or cross-member blueprint imports.
- [x] 7.7 Extend `tests/test_prompt_bundles.py` and `tests/test_skill_suite.py` to check new references, conditional reachability, generated indexes, exact byte totals, existing limits/headroom, and rejection of detailed blueprint text embedded in always-loaded entries.
- [x] 7.8 Update all five skill prompts and blueprint references to distinguish externally owned expected-target admission from member-native parsing, require immutable resolved native receipts, and reject co-shrunk self-authority or caller-invented receipt ids.
- [x] 7.9 Clarify in all five prompts/references that anchor and receipt locator/hash replay is persistent and cache-free, direct member use without external admission is unverified, and alternate targets require separately admitted anchors.
- [x] 7.10 Align all five blueprint references with no public/generic target, attestation, or receipt issuer; no production inline/candidate authoring; exact one-check impact/trace admission; and closed member-native composition replay over resolved evidence.
- [x] 7.11 Add one concise boundary statement to the ResearchGuard envelope reference and each member-owned blueprint reference: the LogicGuard, SourceGuard, TraceGuard, and ExperimentGuard models are member-domain DNA, while one FlowGuard repository root owns ResearchGuard software self-DNA; do not embed repository inventory or seven-layer self-DNA detail in always-loaded entries.
- [x] 7.12 Extend prompt-bundle and skill-suite tests to prove the software/domain-DNA distinction is conditionally reachable, no member advertises itself as a whole-repository blueprint, and entry-byte limits and headroom remain current.
- [x] 7.13 Update the current maintained ResearchGuard prompt source and exact generated projections so an AI discovers one direct-current external-domain-DNA build/inspect path, canonical paper and workflow examples, current-material versus bundle-only claim boundaries, and exact member/behavior/object/impact/reverse drill-down without loading sibling semantic manuals eagerly.
- [ ] 7.14 Run the current ResearchGuard prompt generator under SkillGuard author supervision and prove source, generated `SKILL.md`, `agents/openai.yaml`, prompt manifest, package data, and clean consumer projection agree byte-for-byte; do not hand-edit an installed projection or create a second prompt authority.

## 8. Native and Cross-Member Validation Freeze

- [x] 8.1 Run each affected member's schema, blueprint, purpose, depth, iteration, impact, reverse-trace, CLI, known-good, and known-bad focused tests and repair failures at the native owner.
- [x] 8.2 Run the ResearchGuard envelope, routing, prompt, root CLI, suite identity, and cross-member integration tests and retain every failed, skipped, stale, or not-run owner visibly.
- [x] 8.3 Freeze one current native model/schema/receipt fingerprint set per member and one ResearchGuard envelope/composition contract fingerprint after focused tests pass.
- [x] 8.4 Re-run only obligations affected by any repair and preserve exact-current unaffected resolved receipts; do not use uncertain impact as permission for full execution.
- [ ] 8.5 Record this post-green false-negative as a FlowGuard model miss with the external-admission boundary, immutable-receipt boundary, four same-class members, prior overclaim, and exact closure tests; keep the blocked project-adoption audit visible.
- [x] 8.6 Run four real raw-bytes co-shrink adversarials plus affected ExperimentGuard, LogicGuard, SourceGuard, TraceGuard, umbrella, CLI, prompt, and strict OpenSpec suites; keep full SkillGuard/release validation outside this native repair phase.
- [x] 8.7 Add and run four fresh-process co-shrink tests, four serialized anchor/receipt replay tests, four absent-or-unavailable external-anchor tests, and four alternate-target positives that succeed only with separately admitted anchors.
- [x] 8.8 Add fresh-process arbitrary never-published native-receipt tests for all four members, real ExperimentGuard owner replay, and a real two-member composition proving valid-looking ids/self-hashes cannot replace immutable resolution and readiness remains `composition_blocked`.
- [x] 8.9 Repair all four member-native reverse operations so one current anchored and receipt-resolved qualification completes before target resolution or closure work; atomically suppress ordinary output for incomplete, stale, unverified, or not-run qualification, add unknown-target/call-order/call-count regressions, align LogicGuard impact with current native depth, and keep its blocked `trace_gaps` sorted and duplicate-free.
- [x] 8.10 Replace each member `owner_attestation.py` hash-only replay with a closed-registry invocation of that member's exact current blueprint checker over its externally admitted anchor and independently resolved opaque request/input/result/receipt bytes; derive terminal and gaps from checker output and reject caller-authored substitutes.
- [x] 8.11 Bind every native receipt and attestation to an immutable locator plus actual task/request, admission anchor, member model/input, checker capability/version/entrypoint, producer, raw target material, result, status, and bytes fingerprints; remove every generic or caller-accessible issuer path.
- [x] 8.12 Make all four target adapters accept only replay under a supplied current external expected-target anchor, compare extracted authority bidirectionally with declared universe and native model, reject fresh-process raw-bytes/model/universe/native-evidence co-shrink with recomputed hashes, and retain serialized replay and separately admitted alternate-target positives.
- [x] 8.13 Add semantic false-green regressions for LogicGuard public parent/child realization with exact/missing/stale/foreign leaf receipts and duplicate-free blocked reverse gaps, SourceGuard typed-graph reverse observation closure, TraceGuard exact failure-class denominator, ExperimentGuard procedure dataflow/order, and all-caller-forged ResearchGuard composition.
- [ ] 8.14 Run the prior 455 native/integration tests, the independent auditor's 189 and 24 boundary sets, strict OpenSpec validation, and `git diff --check`; keep FlowGuard self-DNA, generated SkillGuard contracts/TestMesh, installation, Git, versioning, and publication explicitly outside this repair phase.
- [x] 8.15 Add one blocked and one current four-member recursive-composition regression, alternate-producer/key receipt regressions, fixture-import zero-side-effect checks, full transition/oracle contract checks, deterministic bundle round trip, and a fresh-process bundle-only handoff regression.
- [x] 8.16 Add source and isolated-wheel external-domain-DNA regressions proving compact default output below 16 KiB and ten-times smaller than the full artifact, explicit behavior-ID drill-down, repository/target-read denial, native replay, impact/reverse closure, conflict preservation, current-material qualification, and bundle-only anchor-extraction non-licensing.
- [x] 8.17 Run the complete external-domain-DNA false-green attack set, denominator exhaustion, parent mapping exhaustion, structure/member impact and reverse trace, generic conflict/non-conflict/workflow targets, clean-wheel/no-repository-read, canonical packaging, compact size, current-material qualification, and bundle-only self-consistency checks; validate this OpenSpec change strictly and retain FlowGuard project-upgrade, self-DNA, install, version, Git, tag, and release as explicit not-run boundaries.

## 9. FlowGuard Project Record and Single Repository Self-DNA

- [ ] 9.1 Wait until the imported FlowGuard package, editable source checkout, schema, CLI implementation, relevant installed consumer skills, and peer modifications are frozen; record their exact identities before any ResearchGuard FlowGuard write.
- [ ] 9.2 Re-run `python -B -m flowguard project-upgrade --root . --dry-run --json` and verify blockers, artifact changes, semantic rule changes, and proposed files against the accepted preview; stop if the frozen result differs unexpectedly.
- [ ] 9.3 Apply the accepted project upgrade only to its exact declared records and confirm `.flowguard/evidence/` and unrelated peer paths remain untouched.
- [ ] 9.4 Run `python -m flowguard project-audit --root . --json` and the minimum affected project-record revalidation before creating new model authority.
- [ ] 9.5 Produce one independent provider-neutral denominator for the explicit ResearchGuard repository boundary. Admit Python and non-Python source, generated-code boundaries, five skill/prompt surfaces, public CLI/API surfaces, schemas/contracts, configuration, workflows, resources, tests, intent sources, topology edges, external interfaces, and unresolved parse/dynamic findings; give every item one terminal disposition and every behavior-bearing item one primary owner.
- [ ] 9.6 Define one current adapter contract for denominator providers. Retain Python inspection as one convenience adapter, add at least one maintained non-Python/declarative/workflow fixture, and make unknown adapters, unsupported schemas, ambiguous exclusions, duplicate owners, and unparseable surfaces block without a legacy reader, caller subset, Python-only fallback, or automatic broad scan.
- [ ] 9.7 Replace the five-sibling software shape with exactly one ResearchGuard repository software-DNA root and LogicGuard, SourceGuard, TraceGuard, and ExperimentGuard child subtrees. Give the root admission, direct/umbrella routing, composition, envelope, handoff, and authority owners; recursively split every member subtree into multiple concrete owners and express every behavior block as `Input x State -> Set(Output x State)`.
- [ ] 9.8 Bind every model block in both directions to exact code, test, intent, resource, topology, public-surface, native-domain, and external-interface identities. For each test binding record the behavior setup, input, pre-state, expected output, post-state, effect, failure class, and oracle. Build one affected/reverse index that closes through parents, children, readers, writers, consumers, tests, handoffs, resources, and topology dependents and blocks unknown ownership without `run all`.
- [ ] 9.9 Implement the seven readiness layers—evidence qualification, implementation inventory, traceability, independent semantics, model-code-test binding, resource-oracle binding, and static blueprint readiness—with longest-complete-prefix and first-gap reporting. Keep executed tests, external-resource availability, installation, Git, and release currentness as separate claims.
- [ ] 9.10 Keep the canonical self-DNA model, bindings, and current pointer in
  the native repository model directories. Prevent generated inventories,
  projections, pointers, receipts, or transport metadata from entering their
  own denominator; bind any external evidence by identity/owner/locator/
  fingerprint without creating a second DNA authority.
- [ ] 9.11 Update `.flowguard/researchguard_suite_model.json`, `.flowguard/researchguard_suite_model.py`, `.flowguard/researchguard_suite/model.py`, `.flowguard/researchguard_suite/run_checks.py`, `.flowguard/run_researchguard_suite_model.py`, `.flowguard/model-regression-manifest.json`, and the maintenance preflight to consume the single root, four recursively decomposed subtrees, provider-neutral denominator, seven-layer result, bindings, and indexes without promoting member-domain blueprints or generic skill contracts to repository authority.
- [ ] 9.12 Extend `tests/test_suite_model_currentness.py` and integration fixtures to reject a fifth sibling root, missing or opaque member subtrees, duplicate primary owners, unmapped or multiply dispositioned denominator items, stale native bindings, incomplete transition contracts, tests named without input/state/output/effect/oracle bindings, dangling forward/reverse edges, empty coverage dimensions, and member-domain blueprints presented as repository self-DNA.
- [ ] 9.13 Add provider-neutral and external-boundary tests for non-Python surfaces, unsupported/unknown adapters, external resources, provider drift, resource-oracle availability, and zero compatibility/fallback behavior.
- [ ] 9.14 Add loading and impact tests proving ordinary work uses only the
  indexed affected closure, unknown impact blocks without widening, and only
  explicit whole-DNA qualification or proof-gated architecture-reduction and
  release checks inspect the complete denominator.
- [ ] 9.15 Add canonical-boundary tests proving generated output cannot
  self-enter the denominator, an archive cannot authenticate itself, transport
  evidence remains content-addressed, and unrelated repository source does not
  change the native DNA denominator.
- [ ] 9.16 Run affected root/subtree checks against one frozen source, native-receipt, adapter, denominator, and external-resource identity; verify the seven readiness layers, transition contracts, indexes, and first gaps before any architecture-reduction candidate is actionable.
- [ ] 9.17 Perform a read-only architecture-reduction candidate pass only after 9.16 is proof-ready. For every candidate record the observable contract, current primary owner, proposed disposition, affected closure, native evidence, and parity requirements; retain all under-evidenced candidates and add no alias, compatibility, dual-write, or fallback path.
- [ ] 9.18 Build one immutable model revision from the exact current observed root, four member subtrees, denominator, indexes, and external canonical identity; persist all required owner evidence, activate by updating the sole observed pointer last, and audit the resulting model system.

## 10. SkillGuard Author Maintenance

- [ ] 10.1 Re-read the current SkillGuard global registry and this repository's `.skillguard/author-project.json`; freeze the five-member `unit:researchguard-suite` component and check-owner inventory after native and FlowGuard paths stabilize.
- [ ] 10.2 Update each affected `skills/*guard/.skillguard/contract-source.json` with exact prompt, reference, runtime, test, FlowGuard-binding, and projection components while keeping native checks target-owned.
- [ ] 10.3 Compile current contract artifacts directly for all affected members with no compatibility or migration route and verify component-to-owner mapping is complete and unambiguous after the source and FlowGuard toolchain identities are frozen.
- [x] 10.4 Derive an affected-only SkillGuard execution plan with one owner per semantic check; block unmapped files instead of selecting `run all`.

  Source/compile boundary for 10.2-10.4: the source generator now keeps the shared prompt manifest and checker in every member's source component inventory because all five `prompt-load` commands consume them, while assigning those paths only to that member's `prompt-load` owner. `tests/test_prompt_bundles.py` remains an input and command only for the existing ResearchGuard `native-tests` owner. An isolated generation-and-compile preview may prove this source mapping without changing maintained generated artifacts; 10.2 and 10.3 remain open until the five maintained `contract-source.json`, `compiled-contract.json`, and `check-manifest.json` files are formally regenerated and compiled after the FlowGuard/toolchain identities are frozen. The intended closure still retains exactly one existing `native-tests` owner per member, twenty owners and twenty-one obligations in total, no duplicate prompt execution, no compatibility/fallback route, `all_owner_component_ids: []`, and `unknown_mapping_disposition: block`. The sole TestMesh definition remains the repository-root `.skillguard/test-mesh.json`; its manifest and checker belong only to `projection:researchguard-suite-maintenance-definition` and create no semantic owner. No check owner, full validation, aggregation, installation, or release is licensed by this source-only repair.
- [ ] 10.5 Run affected member-native and consumer-contract checks and reserve one frozen maintenance-unit validation owner after every source and toolchain identity is stable; do not execute that full owner here. The post-archive release runbook is its sole execution site.
- [x] 10.6 Replace the repository's raw skill-directory copier and direct filesystem swap with SkillGuard's current consumer-distribution plus target-installation APIs; require exact `consumer-release.json` and returned activation-pointer identities, prepare and reverify all five before activation, reverse-roll back prior members on a later returned failure, and prove the order/failure/currentness boundaries with focused tests.
- [ ] 10.7 Build the five clean consumer projections from the final frozen source and verify that no author contract, receipt, execution-owner, `.skillguard`, FlowGuard evidence, or private path enters consumer runtime.
- [x] 10.8 Harden the suite coordinator against activation and rollback exceptions, malformed successful identities, unsafe or redefinable authority/install paths, predictable manifest temporaries, empty-package false-greens, missing consoles, late retired residuals, and overclaiming stored transaction pointers as current SkillGuard receipt proof.
- [x] 10.9 Reuse the existing ResearchGuard `native-tests` owner for one explicit `consumer-install-transaction` obligation; give prompt manifest/checker governance only to `prompt-load`, execute `tests/test_prompt_bundles.py` only under ResearchGuard native-tests, add one total suite mutation lock, and prove that late failure restores or visibly leaves `cleanup_unconfirmed` for the Python package, all activated consumers, and suite manifest without running generation, compilation, or installation in this source phase.

## 11. Complete implementation, cleanup, and OpenSpec closure before the release freeze

- [ ] 11.1 Freeze a pre-archive ResearchGuard source, dependency, prompt, model, test, installer, and affected-owner inventory sufficient to verify implementation; label every result provisional because OpenSpec archival still changes the repository.
- [ ] 11.2 Consume only proof-ready candidates from 9.17, route public-entrypoint or large-structure contractions through their exact structure owners and lifecycle/release effects through the development-process owner, and keep risky candidates visible rather than deleting them by similarity, size, or an incomplete readiness layer.
- [ ] 11.3 Run every cleanup-affected native, envelope, prompt, consumer-transaction, FlowGuard model, model-test, and package-content check needed to establish implementation parity; inspect a provisional distribution and isolated install without treating either as the final release artifact or installation.
- [ ] 11.4 Run strict OpenSpec validation and verify implementation against all
  capability delta specs, including native semantic independence, opaque
  envelopes, no shared solver/fallback, conditional loading, one repository
  self-DNA root with four recursively decomposed member subtrees, complete
  provider-neutral ownership, seven-layer readiness, bidirectional and
  affected indexes, proof-gated architecture reduction,
  member-domain/software-DNA separation, and transactional current consumer
  installation.
- [ ] 11.5 Confirm every product-behavior task is complete or has an exact blocker and that no test, evidence, install, cleanup, Git, tag, or release claim exceeds its current artifact and owner boundary.
- [ ] 11.6 Sync the accepted delta specs to main specs without reintroducing historical exact-one routing, then archive `strengthen-guard-domain-blueprint-spine` before the final source, model, package, commit, tag, or release freeze. Freeze and record the archived intent-artifact digest. The archived proposal, design, specs, and task list become an immutable intent snapshot: all later lifecycle status belongs only in an out-of-repository release receipt and final report, never in edited archived checkboxes. Archival is an intent-source mutation and invalidates pre-archive whole-repository fingerprints.

## 12. Post-archive release runbook (status is external; never edit the archived checklist)

1. After the immutable FlowGuard release is publicly retrievable, replace the unsatisfiable `flowguard>=0.68.2,<0.69` installation contract with exactly one public current authority resolvable by clean Windows, Linux, CI, and end-user installs. Prefer an official package distribution; otherwise bind one immutable public Git commit/tag or release artifact. Update `pyproject.toml` and only CI steps required by that same identity, prove clean resolution, and add no local-path source, old-PyPI downgrade, optional skip, retry, fallback, or distinct CI/runtime authority. Select the next ResearchGuard patch version by increasing only the third component and update every governed version source.
2. Prepare only the current FlowGuard/member-model binding sources required by repository generators; this preparation is not final model authority. Regenerate and compile every tracked in-repository output first: five SkillGuard contract sources/compiled contracts/check manifests, the sole test mesh, validation-plan source, clean consumer source projections, FlowGuard project records, prompt/version projections, and other version-derived model or release source. Prove exact component/owner mapping and zero author-state leakage.
3. On those fully generated post-archive bytes, independently rebuild the complete denominator and ownership table, recompute affected/reverse indexes, refresh model-code-test/intent/resource/topology bindings, rerun all seven layers, and publish one current native model revision. Model-authority records and receipts are predeclared derived evidence outputs excluded from their own logical fingerprint; after publication rerun the inventory audit and require no undeclared output and no logical identity change.
4. Freeze source, public dependency, prompts, model, test inventory, affected plan, installer, tracked release-role plan, one final repository-suite owner, and one final SkillGuard maintenance-unit owner. Confirm zero descendants remain from interrupted launchers, then execute those two heavy owners exactly once in the foreground. Run final FlowGuard project/model audit, seven-layer qualification, model-test alignment, cleanup parity, package-content preview, and archived-OpenSpec currentness as distinct evidence subjects.
5. Review and stage only owned paths, preserve peer work, and exclude `.flowguard/evidence/**`, caches, author receipts, and unclassified files. Create a local candidate commit without pushing or tagging it.
6. From an isolated clean checkout of that immutable commit, build and inspect the wheel/sdist and five clean consumer assets. Create an out-of-repository commit-binding manifest that joins the verified logical DNA/tree fingerprint to the exact commit SHA, version, asset roles, and checksums without changing the logical DNA. If build or inspection fails, abandon the candidate, repair source, rerun affected and required final gates, and create a new candidate commit.
7. Install the exact artifacts into isolated Python and Codex-skill roots and run package import/schema/direct-member/umbrella/blueprint/prompt/external-DNA/currentness smoke checks. Only then synchronize the authorized local package and five consumers and run read-only installation parity without launching another semantic owner. Any failure returns to candidate repair before remote publication.
8. Recheck remote branch/tag ownership and CI prerequisites. Push only the exact verified candidate, create and push its patch tag, publish the GitHub Release, and upload package/source, clean-consumer, canonical logical DNA, commit-binding manifest, checksum, and release-note assets.
9. Download and verify every asset, then compare source commit, tag, release, package, installed package, five installed skills, external DNA, model authority, checksums, and claim boundaries as distinct identities tied to the same patch.
10. Record terminal lifecycle states and bounded residuals in the out-of-repository immutable release receipt and final report, never by editing archived tasks. Perform the predictive-KB postflight and record one structured observation only when the work exposed a reusable lesson, route gap, miss, or card weakness.
