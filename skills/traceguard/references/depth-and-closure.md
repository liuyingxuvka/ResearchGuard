# Trace Depth and Closure

Broad, complete, causal, deep, robust, or predictive wording requires the canonical inference receipt plus current target-owned storyline-depth execution evidence.

Preserve primary and alternative hypotheses; traces/events/evidence; causal mechanisms, confounders, alternatives, scope; perturbation plan; baseline and perturbed receipts; object and candidate-universe fingerprints; eligible/critical/selected/executed/effective counts; per-kind coverage; critical-uncovered ids; requested/covered scope; and predictive holdout status.

Every perturbation changes typed declarations and recompiles/resolves through the same approved solver. Editing a reported score or rank is invalid. Every critical perturbation must be effective or predeclared as an informative null. One effective probe cannot hide an untested/ineffective critical probe. Solver failure, infeasibility, bad residuals, or unapproved backend is terminal; there is no heuristic fallback.

Broad evidence includes per-object and per-trace child coverage, dynamic floors, early/middle/late rows, maximum unqualified run, evidence uniqueness/lineage, temporal/kind strata, critical flags, causal boundaries, expected-sensitivity checks, and holdout boundary. Counts, kind lists, ranges, or whole-receipt hashes do not prove individual obligations; preserve each native semantic object, evidence reference, and lowercase content hash.

Prediction without a separate native future holdout reports `unsupported_without_native_future_holdout`. Broad/predictive closure needs current request/model identity, no unresolved critical object or ineffective critical probe, complete scope, acceptable solver evidence, and the selected member's current `model_closed_for_task` terminal.
