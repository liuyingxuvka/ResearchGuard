# Validated Template Pack Routing

Target family is `traceguard`; native owner is `traceguard.template_packs`; current catalog revision is `1.0.0` with validated base `purpose`.

Resolve the native trace route first, then obtain the target adapter's current neutral projection. Preserve every candidate and rejection. Zero candidates may use only the base; one gets a read-only preview; many require complete dependencies, compatibility, one field owner, and target-authored dominance or block.

Recompute immediately before applying. Stale request, catalog, route, builder, validator, or content identity blocks writes. Consume the target builder and every native validator receipt. Generated skeletons are non-factual; source rows are not evidence, evidence precedes events, events precede traces, and generated traces are not validated.

Inventory: `case-library`, `causal`, `competing-storyline`, `counterfactual`, `handoff`, `incident`, `purpose`, `research-lineage`, and `technology-progression`. Record harvest after creating or materially deepening a reusable model.
