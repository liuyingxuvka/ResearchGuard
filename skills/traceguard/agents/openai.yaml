interface:
  display_name: "TraceGuard"
  short_description: "Stress-test timelines and competing storylines"
  default_prompt: "Use $traceguard to run the strict task-bound trace loop over actual semantic bindings, current native depth evidence, and a disjoint independent holdout before closure."
