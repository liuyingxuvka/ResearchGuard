interface:
  display_name: "SourceGuard"
  short_description: "Plan evidence discovery and qualify source use"
  default_prompt: "Use $sourceguard by loading the source-model entry first and only the references triggered by retrieval, iteration, broad closure, commands, safe wording, or template work; start the strict search-model loop when a real observation, model miss, deep claim, or addressable gap requires it."
