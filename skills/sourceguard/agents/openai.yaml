interface:
  display_name: "SourceGuard"
  short_description: "Plan evidence discovery and qualify source use"
  default_prompt: "Use $sourceguard to run the strict task-bound search-model loop, compare real retrieval evidence with predictions, rerun native depth checks, and return exact gap lineage and stop reason."
