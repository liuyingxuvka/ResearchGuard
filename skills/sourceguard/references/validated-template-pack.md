# Validated Template Pack Routing

Target family is `sourceguard`; native owner is `sourceguard.template_packs`; current catalog revision is `1.0.0` with validated base `discovery`.

Resolve SourceGuard's native route first, then ask its adapter for a neutral current projection. Preserve all candidates and rejections. Zero candidates may use only the base; one candidate gets a read-only preview; many require complete dependencies, compatibility, one field owner, and target-authored dominance or block.

Recompute before applying. Stale request, catalog, route, builder, validator, or content identity blocks writes. Consume the target builder and all native validator receipts. Template instances plan discovery only; candidates are not evidence, anchors are not events, and handoffs are not downstream validation.

Inventory: `bridge-evidence`, `citation`, `disconfirming`, `discovery`, `gap`, `lineage`, `multimodal-anchor`, and `numeric-provenance`. Record harvest after a reusable model is created or materially deepened.
