interface:
  display_name: "ExperimentGuard"
  short_description: "Recommend discriminating experiments"
  default_prompt: "Use $experimentguard directly for a bounded minimum finite discriminating-set recommendation; trigger its existing strict observation/revision/holdout loop only when later evidence, a prediction-matrix miss, an addressable gap, or explicit predictive/deep closure requires it."
