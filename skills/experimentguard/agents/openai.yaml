interface:
  display_name: "ExperimentGuard"
  short_description: "Recommend discriminating experiments"
  default_prompt: "Use $experimentguard for bounded minimum discriminating sets. For blueprint member-domain DNA load its conditional route and keep external admission separate from native parsing. The ResearchGuard repository software-DNA root is FlowGuard-owned. Never execute experiments."
