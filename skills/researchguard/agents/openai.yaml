interface:
  display_name: "ResearchGuard"
  short_description: "Route research work to one exact Guard owner"
  default_prompt: "Use $researchguard with the exact four current member-authored admission rows to select one research Guard; block zero or multiple matches and preserve the selected member's native receipt and typed handoffs."
