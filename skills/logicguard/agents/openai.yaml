interface:
  display_name: "LogicGuard"
  short_description: "Audit and deepen argument structure"
  default_prompt: "Use $logicguard by selecting one current route and only its declared references. For domain blueprints keep external target admission separate from LogicGuard-native parsing; persistently replay the anchor, request, raw artifact, and immutable receipt locators/hashes without treating cache as authority. Direct unadmitted use is unverified, a different target needs a separate anchor, and no generic issuer or inline candidate-authoring path exists. This is member-domain DNA; the sole ResearchGuard repository software-DNA root is FlowGuard-owned."
