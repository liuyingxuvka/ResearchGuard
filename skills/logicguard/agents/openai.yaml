interface:
  display_name: "LogicGuard"
  short_description: "Audit and deepen argument structure"
  default_prompt: "Use $logicguard to run the strict task-bound argument-model loop, consume current native depth evidence, protect covered claims, test an independent holdout, and close only when no covered gap remains."
