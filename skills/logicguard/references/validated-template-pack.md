# Validated Template Pack Routing

Target family is `family:logicguard-template-packs`; native owner is `owner:logicguard-template-packs`; current catalog revision is `1.0.0` with validated base `logicguard.base`.

Resolve the LogicGuard route first, then ask the target-owned adapter for a current neutral projection. Preserve every candidate and rejection. Zero candidates may use only the declared validated base; one candidate gets a read-only preview; many candidates require complete dependencies, pairwise compatibility, one field owner, and target-authored dominance or block.

Recompute immediately before application. Stale request, catalog, route, builder, validator, or content identity blocks writes. Use the target builder and every target-native validator receipt. Template structure is not domain validity, completion, installation, release, or publication evidence.

Inventory: `logicguard.argument`, `logicguard.base`, `logicguard.deepening`, `logicguard.execution-package`, `logicguard.purpose`, `logicguard.source-library`, `logicguard.structured-artifact`, and `logicguard.synthesis`. Record a harvest disposition after a reusable model is created or materially deepened; keep no-match visible.
